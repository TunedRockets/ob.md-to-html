Remaining known issues:

Looseness:
two criteria:
- "constituent list items are separated by blank lines"
    i.e. we close a list item on newline, then open another


- "constituent list items directly contain two block-level elements with a blank line between them"
    i.e. we close an item on a newline, then open another. (how check?)