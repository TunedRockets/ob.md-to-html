Remaining known issues:

- fenced code blocks do not parse character references
  perhaps a linked list, or list of chars that become strings?
- links and link markers
- list reference inline checker eats too much
- list reference block doesn't evaliate or evaluates incorrectly in spec 527
- HTML block checker is wrong
- test HTML tag checker
- 160 is broken due to above?
- add other types of tag e.g. '<?' to inline
- tabs are not tabs but are tabs.