Remaining known issues:

- loosness has one edge case in 325 and 326
    needs a full redo of the algorithm, probably based on content '\n's

- 491, invalid link destinations should not? be parsed as regular
    and instead be verbatim?